
export  default function CoreConcept({ title, description }) {
  return (
    <div className="container">
      <div>
        <h1>{title}</h1>
        <p>{description}</p>
      </div>
    </div>
  );
}

