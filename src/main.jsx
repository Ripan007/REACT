import React from 'react';
import ReactDOM from 'react-dom/client';
import App from '../src/react-summary/App';
// import { ChakraProvider } from '@chakra-ui/react';
dingle;

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    {/* <ChakraProvider> */}
    <App />
    {/* </ChakraProvider> */}
  </React.StrictMode>
);
